name: ip-leak-guard
on: [push, pull_request]
jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Guard against moat leakage into the open repo
        run: bash scripts/ip-leak-guard.sh
